import React, { FC } from "react";
import Link from "next/link";
import Head from "next/head";

const Layout: FC = () => (
  <div>
    <div>アイウエオ</div>
    <div>かきくけこ</div>
  </div>
);

export default Layout;
